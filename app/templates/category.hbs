<!--<div class="col-xs-5ths resus-col resus-even-col">  Now defined in view! -->
			<h4>{{category.title}}</h4>
			{{#each button in controller.buttons}}
				{{button-display button=button}}
			{{/each}}
		<!--</div>-->